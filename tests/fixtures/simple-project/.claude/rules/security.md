# Security Rules

## Never commit secrets
- API keys must be in environment variables
- Credentials must not be in code

## Code safety
- Always validate user input
- Use parameterized queries for SQL
